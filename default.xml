<?xml version="1.0" encoding="UTF-8"?>
<manifest>
 
  <!-- This is the list of repos we're nabbing things from here... -->
  <remote  name="github"
           fetch="https://github.com" />

  <remote  name="yocto"
	   fetch="http://git.yoctoproject.org" />

  <remote  name="openembedded"
     	   fetch="git://git.openembedded.org/" />

  <!-- Unless overridden by the project line-item, we're grabbing from 
       the head of the master branch for any given git repo for this
       project... -->
  <default revision="refs/heads/master"/>

 <!-- We're using OE/Yocto "Fido" as the project basis.  Anything that
      has to be linked to that branch head needs to use this config will 
      need to be in this grouping with the right branch info in it --> 
 <project name="git/poky" remote="yocto" revision="refs/heads/fido" path="." />
 <project name="git/meta-ti" remote="yocto" revision="refs/heads/fido" path="meta-ti" />
 <project name="git/meta-raspberrypi" remote="yocto" revision="refs/heads/fido" path="meta-raspberrypi" />
 <project name="meta-openembedded" remote="openembedded" revision="refs/heads/fido" path="meta-openembedded" />

 <project name="beagleboard/meta-beagleboard" remote="github" path="meta-beagleboard" />
 <project name="madscientist42/oe-build-config" remote="github" path="build"/>

</manifest>
